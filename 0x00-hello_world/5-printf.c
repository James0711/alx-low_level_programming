#include <stdio.h>
/**
 * main - A program that prints exactly a new line with the printf function
 * Return: 0 (successful)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (o);
}
